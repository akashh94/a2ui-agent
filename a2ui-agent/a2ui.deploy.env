# a2ui-agent service deployment config — office environment.
# Sourced by build.sh and deploy.sh in this directory.
# The personal equivalents live in deploy.personal.env (sourced by
# build.personal.sh and deploy.personal.sh). Picking which file you source
# is what picks which environment you deploy to.
#
# All values use "${VAR:-default}" so you can override per-run:
#   PROJECT_ID=my-project ./deploy.sh

# --- GCP project / region ---------------------------------------------------
export PROJECT_ID="${PROJECT_ID:-labs-gcp-msls-16495-1782829337}"
export REGION="${REGION:-us-east1}"

# --- Model -----------------------------------------------------------------
export AGENT_MODEL="${AGENT_MODEL:-gemini-2.5-flash}"
# Vertex AI model location. Regional endpoint (e.g. us-central1) is required;
# "global" was not reachable for this project's models.
export MODEL_LOCATION="${MODEL_LOCATION:-us-central1}"

# --- Runtime env vars (passed to the Agent Engine) ---------------------------
# Canonical /etcatalog URL of the deployed a2ui-server. The agent's
# get_a2ui_catalog tool calls this URL (and uses it as the ID-token audience).
# After first deploy, set to: https://<a2ui-server-url>/etcatalog
export CATALOG_URL="${CATALOG_URL:-http://localhost:8000/etcatalog}"
